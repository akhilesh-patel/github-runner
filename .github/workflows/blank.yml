name: hello-world
on: push 
jobs:
  first-jobs:
     run-on: self-hosted 
     steps:
      - name: uinstall apache server
        run: yum remove apahe2 -y
      - name: completetly uninstall apache2 server
